package com.sap.cloud.service.flags.demo;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Represents the entry point for Feature Flags demo application.
 */

@SpringBootApplication
public class FeatureFlagsDemoApplication {

	public static void main(String[] args) {
		SpringApplication.run(FeatureFlagsDemoApplication.class, args);
	}
}
